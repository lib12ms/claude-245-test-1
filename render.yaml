services:
  - type: web
    name: kormarc-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir /opt/render/project/src app:app
    envVars:
      - key: ALADIN_API_KEY
        sync: false

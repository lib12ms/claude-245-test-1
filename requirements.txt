flask>=2.0
flask-cors>=3.0
requests>=2.28
gunicorn>=20.0
beautifulsoup4>=4.12
hanja>=0.15
pykakasi>=2.2
jaconv>=0.3
# Streamlit Cloud가 기본으로 읽는 requirements.txt에 streamlit이 없으면
# "streamlit: command not found" 로 앱이 뜨지 않습니다.
streamlit>=1.28.0
openai>=1.0
